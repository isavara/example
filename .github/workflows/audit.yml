name: Audit

on:
  push:
    branches-ignore:
      - main
  pull_request:
    branches-ignore:
      - main
      - 'v*.*'
      
jobs:
  build:
    name: Perform golint and go-staticcheck
    runs-on: gcp-public-runner-label-1
    steps:
      - name: Print PATH
        run: echo $PATH          
      - name: Print PATH
        run: echo $PATH


